trigger:
- master

pool:
  vmImage: 'ubuntu-16.04'

steps:
- script: |
    docker build -t $(dockerId)/eos-docker .
    # docker login -u $(dockerId) -p $(dockerPasswd)
    # docker push $(dockerId)/eos-docker